spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/backend_mysite_db
    username: postgres
    password: "c0mrade"
    driver-class-name: org.postgresql.Driver
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  jpa:
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  output:
    ansi:
      enabled: always
